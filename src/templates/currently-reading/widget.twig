{% set books = craft.currentlyReadingWidget.bookApiService.getCurrentlyReading() %}

{% for book in books %}
	<div class="book">
		<img src="{{ book.coverImageUrl }}" alt="{{ book.title }}">
		<h3>{{ book.title }}</h3>
		<p>{{ book.author }}</p>
	</div>
{% endfor %}
